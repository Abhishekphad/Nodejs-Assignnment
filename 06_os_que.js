const os= require('os');

// OS name
console.log("OS name:", os.type());
// OS version
console.log("OS version:", os.version());
// OS release version
console.log("OS realease version:", os.release());